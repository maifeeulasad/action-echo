name: Export PATH to all steps

on:
  push:
    branches: [ test ]
  pull_request:
    branches: [ test ]

jobs:
  build:
    runs-on: self-hosted
    env:
      PATH: $PATH:/path/you-would-like
    steps:
     - name: 'echo'
       run: echo $PATH
