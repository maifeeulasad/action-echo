name: Export PATH to all steps

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    env:
      PATH: $PATH:/path/you-would-like
    steps:
     - name: 'echo'
       run: echo $PATH
