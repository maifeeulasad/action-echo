name: Export PATH to all steps

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    env:
      PATH: $PATH:/path/you-would-like
    steps:
     - name: 'echo'
       run: echo $PATH
